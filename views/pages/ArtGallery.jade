extends ArtGalleryLayout

block content
  //- div(id='game') &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  //- div(id='blocker')
    //- h3(id='instructions')
    //-    span(style="font-size:40px")='Welcome to Real Faces'
    //-    |<br>
    //-    |To work properly the website requires access to your webcam and to disable your cursor.<br />
    //-    |When the browser asks if you want to allow access (at the top of the window), click allow. <br />
    //-    |To move use W,A,S,D or the arrows. To look around use your mouse. Press Esc to pause. <br />
    //-    |Click to start!
  //- div(id='roomURL')
  //-  | Your room URL: Not yet defined
  div(id='controls')='Move:W,A,S,D       Jump:Space         Orientation(nyi): Arrows/RightMouse               Powered by RealFaces   '
  //- div(id='webcamWarning') Looks like you haven't enabled your webcam! Please re-enable it by first pressing "Esc" to pause, then press "P" on your keyboard, and finally, click "Accept" ! You will not be able to see other videos until you do so!
  div(id='chatBox')
    input(id="chatInput")
    button(id="chatInputButton") Send!
  div(id='tabletControls')
    \(some of these buttons nyi)
    button(id='rise',disabled) rise
    button(id='fall',disabled) fall
    button(id='moveLeft') left
    button(id='moveRight') right
    button(id='moveForward') forward
    button(id='moveBack') backup
    button(id='turnLeft',disabled) turnLeft
    button(id='turnRight',disabled) turnRight
    button(id='turnUp',disabled) turnUp
    button(id='turnDown',disabled) turnDown
    button(id='textChat') textChat
    
    
    
  video(id='localVideo')
  span(id='remotesVideos')
